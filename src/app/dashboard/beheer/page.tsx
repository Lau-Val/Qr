import { DealBeheerClient } from "./DealBeheerClient";

export const metadata = {
  title: "Dealbeheer — BarBoost",
  description: "Deals instellen voor de bar",
};

export default function DealBeheerPage() {
  return <DealBeheerClient />;
}
