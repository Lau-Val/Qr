import { GuestFlow } from "../GuestFlow";

export const metadata = {
  title: "Salon — BarBoost",
  description: "Prijzen winnen bij de kapper (demo-template)",
};

export default function GastKapperPage() {
  return <GuestFlow templateId="kapper" />;
}
