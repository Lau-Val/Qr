import { GuestFlow } from "./GuestFlow";

export const metadata = {
  title: "Gast — BarBoost",
  description: "QR-deal flow voor gasten",
};

export default function GastPage() {
  return <GuestFlow />;
}
